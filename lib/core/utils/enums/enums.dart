enum NavigationLabelsDrawer {
  laundries,
  favorite,
  myOrder,
  ordersLog,
  settings,
  termsConditions,
  systemMethod
}

enum LaundryFilter { closer, topRated }

enum ServiceType {
  heavy,
  light,
}

enum OrderType {
  oneWay,
  twoWay,
}


enum PayTypes{
  cash,
  masterCard,
  visaCard,
  paypal,
  googlePay
}
